# Source Code

This directory contains the Remotion source code for the Val-X Reel.

## Structure

- `scenes/` - Video scenes (7 scenes)
- `components/` - Reusable React components (8 components)
- `styles/` - Theme configuration

## Usage

```bash
cd val-x-reel
npm start
```
